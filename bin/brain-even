#!/usr/bin/env node

import * as step from '../src/index.js';


const nameUser = step.saveName(step.getName());
const generateRandomNum = step.generateRandomNum();
step.game(generateRandomNum, nameUser);